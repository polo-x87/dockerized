# Copy to ~/.config/op/deepseek.env after filling 1Password references.
# DeepSeek is not installed in devbox-base; use with npx or your own client:
#   op run --env-file=~/.config/op/deepseek.env -- npx -y run-deepseek-cli
DEEPSEEK_API_KEY=op://Personal/DeepSeek/api_key
